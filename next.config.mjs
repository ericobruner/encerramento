/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['plus.unsplash.com', 'images.unsplash.com'], // Permitir o domínio do Unsplash
    },
};

export default nextConfig;
